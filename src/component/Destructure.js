import React from 'react'

function Destructure({title, price}) {
    // use the curly braces to destructure in the argument
    

    return (
        <div className="john">
            <h2> DESTRUCTURING</h2>

            <h2> Your favourite Handbook {title } costs £{price}</h2>

            

            
        </div>
    )
}

export default Destructure
